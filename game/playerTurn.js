/**
 * Handles player input and actions (move, attack, heal, shoot) for the core game logic.
 * All logic is now encapsulated in the Player class as methods.
 * No UI, rendering, or animation logic is included.
 * @file game/playerTurn.js
 *
 * See Player class for:
 *   - Player.prototype.moveOrAttack(targetX, targetY, gameState)
 *   - Player.prototype.heal(gameState)
 *   - Player.prototype.shoot(direction, dirString, gameState)
 */